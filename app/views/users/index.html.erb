<h1>Users</h1>

<% if !logged_in? %>
  <%= link_to 'Sign Up &raquo;', signup_path %>
<% end %>

<% if !@users.blank? %>
  <ul>
  <% for user in @users %>
    <li><%= render :partial => user %></li>
  <% end %>  
  </ul>
  
<% else %>
  <p>No Users.</p>
<% end %>
