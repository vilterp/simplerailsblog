<%= link_to 'New Post', new_post_path if logged_in? %>

<% if !@posts.blank? %>
  <% for post in @posts.reverse %>
    <%= render :partial => post %>
  <% end %>
<% else %>
  <p>No posts.</p>
<% end %>
